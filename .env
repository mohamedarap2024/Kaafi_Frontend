VITE_API_URL="http://localhost:5000/api"

# Supabase environment variables removed
# Backend DB environment variable does not belong in frontend .env
# If you need to run the backend locally, copy Backend/.env.example to Backend/.env